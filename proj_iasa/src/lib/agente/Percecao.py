from abc import ABC

"""
(A interface) Representa o comportamento de perceção do agente. 
Foi utilizada uma interface, pois não é necessária a estrutura de uma classe para este existir.

@param ABC (biblioteca Abstract Base Classes) para que a classe seja abstrata.
"""
class Percecao(ABC):
    """Interface que representa uma perceção"""
